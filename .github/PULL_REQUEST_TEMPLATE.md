## What and why

<!-- What does this change, and why is it needed? -->

## Checklist

- [ ] `npm run check` passes
- [ ] Tests added or updated (tests never touch the network; use the mock in `test/support/`)
- [ ] The PowerShell edition still matches the npm edition, if behaviour changed
- [ ] README (both languages) and `CHANGELOG.md` updated, if behaviour changed
- [ ] If Microsoft's endpoints are involved: I say which live response I observed
- [ ] The signature check is not weakened
